import { ReactNode } from "react";

export default function GuildFormMenu(props: { children: ReactNode }) {
    return (
        <>
            <main className="">

            </main>
        </>
    )
}